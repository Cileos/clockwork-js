thead
  tr
    th Teams
    each x in structure.formattedXValues
      th
        = x
tbody
  each row in structure.rows
    tr
      th= row.head
      each cell in row.cells
        td: ul
          each item in cell.items
            li= item.shortPeriod
